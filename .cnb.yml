# Fork overlay：镜像由 GitHub Actions 在 linux/amd64 上构建，再拷贝到 CNB。
# 不要在 CNB 二次 docker build，默认 runner 会把清单标成 amd64、可运行层却是 arm64。

$:
  api_trigger:
    - name: 跳过镜像构建
      stages:
        - name: 说明
          script: |
            echo "Jeepay 镜像由 GitHub mirror-release 构建并拷贝到 CNB，不在此重建。"
